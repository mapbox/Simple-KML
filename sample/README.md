# Simple KML Sample Project

This project parses the included example.kml, which contains a couple cities and a polygon using them as vertices, and draws it on a map view. The points respond to taps using the KML object titles as labels.